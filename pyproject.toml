[project]
name = "lld-python"
version = "1.0.0"
description = "Low Level Design practice problems in Python OOP"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["lld*"]
